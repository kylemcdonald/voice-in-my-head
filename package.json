{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "buildcss": "npx tailwindcss -i ./tailwind/input.css -o ./static-pages/style.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
